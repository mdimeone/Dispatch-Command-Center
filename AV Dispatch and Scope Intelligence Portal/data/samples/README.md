Sample data for the prototype should live here.

Recommended next files:
- `dispatch.sample.xlsx`
- `cases.sample.xlsx`
- `bom.sample.xlsx`

The current scaffold serves normalized in-memory records from `src/lib/data/sample-data.ts` so the UI,
API contracts, and service layer can move forward before workbook parsing is implemented.
