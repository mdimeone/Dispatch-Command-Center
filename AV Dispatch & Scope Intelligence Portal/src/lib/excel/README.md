This folder is reserved for the temporary Excel ingestion layer.

The current scaffold uses normalized sample data so the UI and API shape can be built first.
When Excel files are introduced, add parsers here that translate workbook structures into the
domain interfaces in `src/types/domain.ts`.
